# Deprecated Tauri UI

The Tauri/React frontend has been removed from production release scope.

PulseBoost C++ now treats the Qt/QML desktop application as the only production UI. Historical Tauri work should stay out of release builds, packaging, and runtime validation unless a future migration explicitly reactivates it.
